from pathlib import Path

CONFIG_FILE_PATH = Path("config/config.yaml")
PARAMS_FILE_PATH = Path("params.yaml")

#Path() is basically converting a string (like a file name) into a real file path object that Python can understand and work with smartly.